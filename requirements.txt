# Ingestion runtime.
#
# Extraction runs on Snowflake warehouse compute via Snowpark Connect for Spark,
# so there is no local Spark, no JVM and no Maven jars. snowpark-connect pins its
# own snowflake-snowpark-python range; do not pin it here as well.
snowpark-connect==1.43.0
snowflake-connector-python==4.7.2
python-dotenv==1.2.2
requests==2.34.2
